## План работ по замене фронтенда

### Этап 1: Подготовка и очистка

- [x] 1.1. **Резервное копирование:** Создать временную резервную копию текущих папок `src` и `public`.
- [x] 1.2. **Удаление:** Полностью удалить содержимое папок `src` и `public` в проекте `localtour-backend`.
- [x] 1.3. **Удаление зависимостей:** Удалить из `package.json` старые фронтенд-зависимости (например, `react-scripts`, `@testing-library/react`).

### Этап 2: Копирование и установка

- [x] 2.1. **Копирование файлов:** Скопировать все файлы и папки из `C:\Users\User\damdaman\localtour-2` в корневую директорию `localtour-backend`, за исключением файлов, специфичных для бэкенда (`functions`, `firebase.json` и т.д.).
- [x] 2.2. **Обновление `package.json`:** Объединить `dependencies` и `devDependencies` из `package.json` нового фронтенда с `package.json` в `localtour-backend`.
- [x] 2.3. **Установка зависимостей:** Выполнить `npm install` для установки всех новых и обновленных зависимостей.

### Этап 3: Интеграция и настройка

- [x] 3.1. **Настройка Vite:** Адаптировать `vite.config.ts` для корректной работы в структуре проекта `localtour-backend`.
- [x] 3.2. **Интеграция Firebase:** Создать или обновить файл конфигурации Firebase (`src/firebase.ts`) с актуальными ключами и настройками для взаимодействия с эмуляторами и продакшн-средой.
- [x] 3.3. **Проверка сборки:** Выполнить команду `npm run build`, чтобы убедиться, что проект собирается без ошибок с помощью Vite.
- [x] 3.4. **Проверка запуска:** Выполнить `npm run dev` (или аналогичную команду Vite) и проверить, что приложение запускается локально.

### Этап 4: Финализация

- [x] 4.1. **Очистка:** Удалить все временные файлы и папки (например, `src_backup`).
- [x] 4.2. **Обновление документации:** Обновить `README.md` с новыми инструкциями по сборке и запуску проекта.
- [x] 4.3. **Архивация:** Заархивировать выполненную задачу в OpenSpec.
